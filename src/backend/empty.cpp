// Placeholder translation unit: CMake requires the "backend" STATIC target to
// have at least one source. Real sources live in gpu/*.cu and are added only
// when MGLET_OFFLOAD_BACKEND is cuda or hip.
